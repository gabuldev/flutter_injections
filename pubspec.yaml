name: flutter_injections
description: A lightweight dependency injection package for Flutter that enables context-free dependency management and modular architecture.
version: 1.1.2
homepage: https://gabul.dev
repository: https://github.com/gabuldev/flutter_injections
issue_tracker: https://github.com/gabuldev/flutter_injections/issues

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

topics:
  - dependency
  - di
  - injection
 
